apiVersion: v1
kind: Service
metadata:
  name: my-frontend
spec:
  type: {{ .Values.service.type }}
  selector:
    app: my-frontend
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
